# 文件夹索引生成器 (Folder Index Generator)

## 功能

为包含代码文件的文件夹生成 `FOLDER_INDEX.md`,列出文件夹内所有文件及其 Input/Output/Pos 信息。

## 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `folderPath` | string | 文件夹绝对路径 |
| `files` | array | 文件夹内文件列表及其分析结果 |
| `language` | string | 语言代码 (`zh-CN` / `en-US`) |
| `LANG` | object | 语言对象 |

## 输出

| 字段 | 类型 | 说明 |
|------|------|------|
| `indexContent` | string | 生成的 FOLDER_INDEX.md 内容 (Markdown格式) |

## 索引模板

### 中文模板 (zh-CN)

```markdown
# {folderName}

🔄 **自引用提醒**: 当此文件夹内容变化时,更新此索引

## 📁 文件夹定位

**Pos**: {folderPos}

**职责**: {folderResponsibility}

---

## 📄 文件列表

### {fileName1}

- **Input**: {input}
- **Output**: {output}
- **Pos**: {pos}

### {fileName2}

- **Input**: {input}
- **Output**: {output}
- **Pos**: {pos}

...

---

## 🔗 依赖关系

### 内部依赖

{folderName} 内的文件相互依赖:

- `{file1}` → `{file2}`, `{file3}`
- `{file2}` → `{file4}`

### 外部依赖

{folderName} 依赖以下外部模块:

- `{externalDep1}`
- `{externalDep2}`

---

## 📊 统计信息

- **文件总数**: {totalFiles}
- **主要语言**: {primaryLanguage}
- **架构层级**: {layers}

---

*此索引由 project-multilevel-index 自动生成*
```

---

### 英文模板 (en-US)

```markdown
# {folderName}

🔄 **Self-Reference Reminder**: Update this index when folder contents change

## 📁 Folder Position

**Pos**: {folderPos}

**Responsibility**: {folderResponsibility}

---

## 📄 File List

### {fileName1}

- **Input**: {input}
- **Output**: {output}
- **Pos**: {pos}

### {fileName2}

- **Input**: {input}
- **Output**: {output}
- **Pos**: {pos}

...

---

## 🔗 Dependencies

### Internal Dependencies

Files within {folderName} depend on each other:

- `{file1}` → `{file2}`, `{file3}`
- `{file2}` → `{file4}`

### External Dependencies

{folderName} depends on the following external modules:

- `{externalDep1}`
- `{externalDep2}`

---

## 📊 Statistics

- **Total Files**: {totalFiles}
- **Primary Language**: {primaryLanguage}
- **Architecture Layers**: {layers}

---

*This index is automatically generated by project-multilevel-index*
```

---

## 生成逻辑

### 步骤 1: 提取文件夹信息

```
function extractFolderInfo(folderPath, files):
  folderName = 获取文件夹名(folderPath)  // 如 "services"

  // 推断文件夹定位 (基于文件的 Pos)
  fileLayers = files.map(f => extractLayer(f.pos))

  // 统计最常见的层级
  layerCounts = countOccurrences(fileLayers)
  mostCommonLayer = getMostCommon(layerCounts)

  folderPos = mostCommonLayer  // 如 "业务层" / "Business Layer"

  // 推断职责 (基于文件夹名)
  folderResponsibility = inferResponsibility(folderName, language)
  // 如 "services" → "业务逻辑" / "Business Logic"

  return {
    folderName,
    folderPos,
    folderResponsibility
  }
```

**职责推断**:
```
function inferResponsibility(folderName, language):
  词典映射 = {
    "controllers": { zh: "控制器", en: "Controllers" },
    "services": { zh: "业务逻辑", en: "Business Logic" },
    "models": { zh: "数据模型", en: "Data Models" },
    "components": { zh: "UI组件", en: "UI Components" },
    "utils": { zh: "工具函数", en: "Utility Functions" },
    "middlewares": { zh: "中间件", en: "Middleware" },
    "config": { zh: "配置", en: "Configuration" },
    "tests": { zh: "测试", en: "Tests" },
    ...
  }

  如果 folderName 在词典中:
    return language == "zh-CN" ? 词典[folderName].zh : 词典[folderName].en
  否则:
    // 驼峰转换
    return formatFolderName(folderName)
```

---

### 步骤 2: 生成文件列表部分

```
function generateFileList(files, language):
  // 按文件名排序
  sortedFiles = files.sort((a, b) => a.fileName.localeCompare(b.fileName))

  fileListMarkdown = []

  对每个文件:
    section = `### ${file.fileName}\n\n`
    section += `- **Input**: ${file.input.join(", ") || (language == "zh-CN" ? "无" : "None")}\n`
    section += `- **Output**: ${file.output.join(", ") || (language == "zh-CN" ? "无" : "None")}\n`
    section += `- **Pos**: ${file.pos}\n`

    fileListMarkdown.push(section)

  return fileListMarkdown.join("\n")
```

**示例输出**:
```markdown
### userService.js

- **Input**: axios, ./models/User, lodash
- **Output**: UserService, createUser(), getUser()
- **Pos**: 业务层-用户服务

### authService.js

- **Input**: jwt, bcrypt, ./models/User
- **Output**: AuthService, login(), register()
- **Pos**: 业务层-认证服务
```

---

### 步骤 3: 分析依赖关系

```
function analyzeDependencies(files, folderPath):
  internalDeps = {}   // 内部依赖
  externalDeps = new Set()  // 外部依赖

  对每个文件:
    对每个依赖 in file.input:
      如果依赖是相对路径:
        // 解析为绝对路径
        resolvedPath = resolvePath(folderPath, file.fileName, 依赖)

        如果 resolvedPath 在同一文件夹内:
          // 内部依赖
          如果 internalDeps[file.fileName] 不存在:
            internalDeps[file.fileName] = []

          targetFile = 获取文件名(resolvedPath)
          internalDeps[file.fileName].push(targetFile)
        否则:
          // 跨文件夹依赖,视为外部
          externalDeps.add(依赖)
      否则:
        // 第三方依赖
        externalDeps.add(依赖)

  return { internalDeps, externalDeps: Array.from(externalDeps) }
```

---

### 步骤 4: 生成依赖关系部分

```
function generateDependenciesSection(internalDeps, externalDeps, language):
  section = []

  // 内部依赖
  if (Object.keys(internalDeps).length > 0):
    if (language == "zh-CN"):
      section.push("### 内部依赖\n")
      section.push(`${folderName} 内的文件相互依赖:\n`)
    else:
      section.push("### Internal Dependencies\n")
      section.push(`Files within ${folderName} depend on each other:\n`)

    对每个文件 in internalDeps:
      deps = internalDeps[文件].join(", ")
      section.push(`- \`${文件}\` → ${deps}`)

    section.push("")

  // 外部依赖
  if (externalDeps.length > 0):
    if (language == "zh-CN"):
      section.push("### 外部依赖\n")
      section.push(`${folderName} 依赖以下外部模块:\n`)
    else:
      section.push("### External Dependencies\n")
      section.push(`${folderName} depends on the following external modules:\n`)

    // 去重和排序
    uniqueDeps = Array.from(new Set(externalDeps)).sort()

    // 限制显示数量 (避免过长)
    显示数量 = Math.min(uniqueDeps.length, 20)

    for (i = 0; i < 显示数量; i++):
      section.push(`- \`${uniqueDeps[i]}\``)

    if (uniqueDeps.length > 20):
      剩余 = uniqueDeps.length - 20
      if (language == "zh-CN"):
        section.push(`- ...(+${剩余}项)`)
      else:
        section.push(`- ...(+${剩余} more)`)

  return section.join("\n")
```

**示例输出**:
```markdown
### 内部依赖

services 内的文件相互依赖:

- `userService.js` → `authService.js`
- `orderService.js` → `userService.js`, `productService.js`

### 外部依赖

services 依赖以下外部模块:

- `axios`
- `lodash`
- `./models/User`
- `./utils/validator`
```

---

### 步骤 5: 生成统计信息部分

```
function generateStatistics(files, language):
  totalFiles = files.length

  // 统计主要语言
  extensions = files.map(f => 获取扩展名(f.fileName))
  extensionCounts = countOccurrences(extensions)
  primaryExtension = getMostCommon(extensionCounts)
  primaryLanguage = mapExtensionToLanguage(primaryExtension)

  // 统计架构层级
  layers = files.map(f => extractLayer(f.pos))
  uniqueLayers = Array.from(new Set(layers))

  if (language == "zh-CN"):
    stats = `- **文件总数**: ${totalFiles}\n`
    stats += `- **主要语言**: ${primaryLanguage}\n`
    stats += `- **架构层级**: ${uniqueLayers.join(", ")}\n`
  else:
    stats = `- **Total Files**: ${totalFiles}\n`
    stats += `- **Primary Language**: ${primaryLanguage}\n`
    stats += `- **Architecture Layers**: ${uniqueLayers.join(", ")}\n`

  return stats
```

**语言映射**:
```
function mapExtensionToLanguage(extension):
  映射 = {
    ".js": "JavaScript",
    ".ts": "TypeScript",
    ".py": "Python",
    ".java": "Java",
    ".rs": "Rust",
    ".go": "Go",
    ...
  }

  return 映射[extension] || extension
```

**示例输出**:
```markdown
## 📊 统计信息

- **文件总数**: 15
- **主要语言**: JavaScript
- **架构层级**: 业务层
```

---

### 步骤 6: 组装完整索引

```
function generateFolderIndex(params):
  { folderPath, files, language, LANG } = params

  // 提取文件夹信息
  folderInfo = extractFolderInfo(folderPath, files)

  // 分析依赖
  { internalDeps, externalDeps } = analyzeDependencies(files, folderPath)

  // 生成统计
  stats = generateStatistics(files, language)

  // 选择模板
  template = language == "zh-CN" ? 中文模板 : 英文模板

  // 生成各部分
  fileListSection = generateFileList(files, language)
  depsSection = generateDependenciesSection(internalDeps, externalDeps, language)
  statsSection = stats

  // 填充模板
  indexContent = template
    .replace("{folderName}", folderInfo.folderName)
    .replace("{folderPos}", folderInfo.folderPos)
    .replace("{folderResponsibility}", folderInfo.folderResponsibility)
    .replace("{fileList}", fileListSection)
    .replace("{dependencies}", depsSection)
    .replace("{statistics}", statsSection)

  return indexContent
```

---

## 完整生成示例

### 示例输入

```json
{
  "folderPath": "/path/to/project/src/services",
  "files": [
    {
      "fileName": "userService.js",
      "input": ["axios", "./models/User", "lodash"],
      "output": ["UserService", "createUser()", "getUser()"],
      "pos": "业务层-用户服务"
    },
    {
      "fileName": "authService.js",
      "input": ["jwt", "bcrypt", "./models/User", "./userService"],
      "output": ["AuthService", "login()", "register()"],
      "pos": "业务层-认证服务"
    }
  ],
  "language": "zh-CN",
  "LANG": { ... }
}
```

### 示例输出

```markdown
# services

🔄 **自引用提醒**: 当此文件夹内容变化时,更新此索引

## 📁 文件夹定位

**Pos**: 业务层

**职责**: 业务逻辑

---

## 📄 文件列表

### authService.js

- **Input**: jwt, bcrypt, ./models/User, ./userService
- **Output**: AuthService, login(), register()
- **Pos**: 业务层-认证服务

### userService.js

- **Input**: axios, ./models/User, lodash
- **Output**: UserService, createUser(), getUser()
- **Pos**: 业务层-用户服务

---

## 🔗 依赖关系

### 内部依赖

services 内的文件相互依赖:

- `authService.js` → `userService.js`

### 外部依赖

services 依赖以下外部模块:

- `./models/User`
- `axios`
- `bcrypt`
- `jwt`
- `lodash`

---

## 📊 统计信息

- **文件总数**: 2
- **主要语言**: JavaScript
- **架构层级**: 业务层

---

*此索引由 project-multilevel-index 自动生成*
```

---

## 边界情况处理

### 1. 单文件文件夹

```
如果 files.length == 1:
  跳过内部依赖部分 (无意义)

  生成:
  ## 📄 文件列表

  ### {唯一文件}
  ...

  ## 🔗 依赖关系

  ### 外部依赖
  ...
```

---

### 2. 无依赖文件夹

```
如果 externalDeps.length == 0:
  依赖关系部分显示:

  ## 🔗 依赖关系

  此文件夹无外部依赖。
```

---

### 3. 混合语言文件夹

```
如果文件夹包含多种语言:
  主要语言 = 最常见的语言

  统计信息部分显示:
  - **主要语言**: JavaScript (8 files), Python (3 files)
```

---

### 4. 深层嵌套文件夹

```
如果 folderPath 很深:
  folderName = 仅显示最后一级文件夹名

  如: /a/b/c/d/services → "services"

  在文件夹定位部分添加完整路径:
  **完整路径**: a/b/c/d/services
```

---

## 性能优化

### 1. 依赖解析缓存

```
pathResolveCache = {}

function resolvePath(folderPath, fileName, dependency):
  key = `${folderPath}:${fileName}:${dependency}`

  if (pathResolveCache[key]):
    return pathResolveCache[key]

  resolved = doResolve(folderPath, fileName, dependency)
  pathResolveCache[key] = resolved

  return resolved
```

### 2. 模板预编译

```
templateCache = {
  "zh-CN": 预编译中文模板,
  "en-US": 预编译英文模板
}

function getTemplate(language):
  return templateCache[language]
```

---

## 总结

文件夹索引生成器为每个文件夹创建结构化的索引文档。

**关键特性**:
- ✅ 列出文件夹内所有文件
- ✅ 显示文件的 Input/Output/Pos
- ✅ 分析内部和外部依赖关系
- ✅ 提供统计信息
- ✅ 嵌入自引用提醒
- ✅ 双语支持

**生成的索引包含**:
1. 文件夹定位和职责
2. 文件列表 (含详细信息)
3. 依赖关系图
4. 统计信息

**下一步**: 参见 [project-index-gen.md](./project-index-gen.md) 了解项目索引生成逻辑
